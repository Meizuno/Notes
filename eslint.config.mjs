// Flat ESLint config. The Nuxt-aware base is generated into
// .nuxt/eslint.config.mjs by @nuxt/eslint; we extend it here so
// project-specific rules live in one place.
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt()
